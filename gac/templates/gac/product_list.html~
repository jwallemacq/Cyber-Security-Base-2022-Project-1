    <h1>Products</h1>

    {% if product_list %}
    <ul>

      {% for product in product_list %}
              {{ product.product_text }}
      </li>
      {% endfor %}
    </ul>

    {% else %}
      <p>There are no products.</p>
    {% endif %}

